{
  "name": "matrix-typescript-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "start": "node dist/app.js",
    "dev:build": "tsc -w --preserveWatchOutput",
    "dev:start": "nodemon dist/app.js",
    "dev": "concurrently npm:dev:start npm:dev:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@matrix-org/olm": "^3.2.15",
    "dotenv": "^16.3.1",
    "matrix-js-sdk": "^28.1.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.6.3",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "prettier": "3.0.3"
  }
}
